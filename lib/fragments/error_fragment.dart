import 'package:flutter/material.dart';

class ErrorFragment extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return ErrorWidget(
      const Text('Fragment not found'),
    );
  }
}